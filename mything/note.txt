-終端指令-
cd..            //上一層
cd 資料夾名     //去往該路徑

npm install(npm i)  載入package.json的模組

-上傳檔案-
open virtual Studio code
把檔案拉過去
輸入git add .      //上傳全部檔案
git commit -m "(輸入備註)"
點Git graph
輸入Git push       //上傳github